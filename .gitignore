# Claude Code local logs (machine-specific session metadata)
.claude/logs/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp

# Environment / secrets
.env
.env.*
*.local

# Node / build artifacts (root-level only — anchored so docs/build/ is NOT ignored)
node_modules/
/dist/
/build/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
