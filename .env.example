# Copy this to .env (which is gitignored and never committed):
#     cp .env.example .env
# Then put your real key in .env. The spikes/app load it automatically via `dotenvy`,
# so it persists across terminals and reboots — no manual `export` needed.

ANTHROPIC_API_KEY=sk-ant-api03-REPLACE_ME
