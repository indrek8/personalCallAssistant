# Throwaway artifacts of the M0 spikes — none of this should be committed.
# (The repo-root .gitignore already ignores spikes/target/.)

/target/
/models/          # ggml model blobs fetched by fetch-model.sh (hundreds of MB)
*.wav             # recorded / generated audio (sample.wav, dual_capture.wav, ...)
